# chap-secrets — user/pass cho PPP (MS-CHAPv2) trên accel-ppp
# Định dạng: <client>  <server>  <secret>  <IP>
#   client = username; server = '*' (mọi service); secret = password; IP = '*' (cấp từ pool)
# Lab dùng user 'vpn' / pass 'vpn' (khớp default demo VPN Gate của TqkLibrary).
#
# Client demo:
#   SSTP : --vpn sstp://vpn:vpn@<VM_IP>
#   L2TP : --vpn l2tp://vpn:vpn@<VM_IP>?psk=vpn
#   PPTP : (driver Drivers.Pptp; demo chưa wire scheme pptp — xem README)
vpn  *  vpn  *
